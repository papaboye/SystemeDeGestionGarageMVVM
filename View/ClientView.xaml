<Window x:Class="TravailPratique2.View.ClientView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TravailPratique2.View"
        mc:Ignorable="d"
        Title="Gestion de garage — Client" Height="520" Width="980"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <TabControl>
            <TabItem Header="Consulter Voiture">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Acheter Voiture" Click="Acheter_Click"/>
                    </StackPanel>
                    <DataGrid Grid.Row="1" ItemsSource="{Binding Voitures}" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Marque" Binding="{Binding marque}"/>
                            <DataGridTextColumn Header="Modèle" Binding="{Binding modele}"/>
                            <DataGridTextColumn Header="Année" Binding="{Binding annee}"/>
                            <DataGridTextColumn Header="Categorie" Binding="{Binding categorie}"/>
                            <DataGridTextColumn Header="Prix" Binding="{Binding prixAproximatif}"/>
                            <DataGridTextColumn Header="Carburant" Binding="{Binding typeCarburant}"/>
                            <DataGridTextColumn Header="Kilometrage" Binding="{Binding kilometrage}"/>
                            <DataGridTextColumn Header="Couleur" Binding="{Binding couleur}"/>
                            <DataGridTextColumn Header="Transmission" Binding="{Binding transmission}"/>
                            <DataGridTextColumn Header="Vin" Binding="{Binding vin}"/>
                            <DataGridTextColumn Header="Proprietaire" Binding="{Binding proprietaireActuel}"/>
                            <DataGridTextColumn Header="État" Binding="{Binding etatGeneral}"/>
                            <DataGridTextColumn Header="Date Achat" Binding="{Binding dateAchat}"/>
                            <DataGridTextColumn Header="Révision" Binding="{Binding derniereRevision}"/>
                            <DataGridTextColumn Header="Garantie" Binding="{Binding garantitRestant}"/>
                            <DataGridTextColumn Header="Assurance" Binding="{Binding assurance}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="Consulter Piece">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Catalogue des pièces disponibles" FontWeight="SemiBold"/>
                    <DataGrid Grid.Row="1" ItemsSource="{Binding Pieces}" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Nom Piece" Binding="{Binding nom_de_piece}"/>
                            <DataGridTextColumn Header="Prix Approximatif" Binding="{Binding prix_approx}"/>



                        </DataGrid.Columns>
                    </DataGrid>

                </Grid>
            </TabItem>
            <TabItem Header="Historique Reparation">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Demander Reparation" Click="DemanderReparation_Click"/>
                    </StackPanel>
                    <DataGrid Grid.Row="1" ItemsSource="{Binding Reparations}" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Categorie" Binding="{Binding categorie}"/>
                            <DataGridTextColumn Header="Type de réparation" Binding="{Binding type}"/>
                            <DataGridTextColumn Header="Réparation associée" Binding="{Binding reparation_associee}"/>
                            <DataGridTextColumn Header="Coût de la réparation" Binding="{Binding cout}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
        
    </Grid>
</Window>
